@model SistemStock.Models.ViewModels.ClienteViewModel
@{
    ViewData["Title"] = "Eliminar Cliente";
}
<h1>Eliminar Cliente</h1>
<h3>¿Está seguro que desea eliminar este cliente?</h3>
<div>
    <dl class="row">
        <dt class="col-sm-2">@Html.DisplayNameFor(model => model.Nombre)</dt>
        <dd class="col-sm-10">@Html.DisplayFor(model => model.Nombre)</dd>
    </dl>
    <form asp-action="Delete">
        <input type="hidden" asp-for="id" />
        <input type="submit" value="Eliminar" class="btn btn-danger" /> |
        <a asp-action="Index">Volver al listado</a>
    </form>
</div>
